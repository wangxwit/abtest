# 原型逐版验收记录（归档）

保留 2026-09-23 整理前的逐版证据；正文中的“当前”“本轮”均指各节当时版本。旧功能可能已被替代，不能作为现行规范。

[验收摘要](../VERIFICATION.md) · [现行文档](../README.md)

---

# 原型验收记录

最新完成：2026-09-12，v21 全页实验草稿工作区，详见文末。早期结果保留为历史记录。

日期：2026-09-06。范围：中文企业实验平台与 Google 递归层域模型交互原型。

## 自动化检查

- `npm run build`：TypeScript 与 Vite 生产构建通过。单入口 JS 约 717 kB，Vite 提示后续可拆分图表依赖；不影响本次本地运行。
- `npm test`：20 项通过，0 失败。包括 2,500 个用户的确定性、递归层域路由和并行分支检查，以及 800 个用户的升级前后已有实验分配保持检查。
- 结构：根域唯一默认层；域和层交替连接；兄弟域与父层直接实验共桶互斥；节点标识、环、孤立节点及不合法参数集合被拒绝。
- 参数：孙域流量沿祖先连乘，`*` 仅继承父层范围；禁止越过父层参数或同域参数重复归层。非重叠后代仍须保持单一实验分支。
- 稳定性：新增子域仅使用连续空闲桶；暂停与待审保留位置，未知受众不执行且不补桶；祖先其他分支决策不变。
- 迁移：旧 [60%,70%) 实验保留原位置，演示子域改用 [70%,90%) 与 [90%,100%)；碎片或容量不足时跳过对应演示分支及孤立种子；已有拓扑优先；v1 先按平铺模型归一化。
- 另行验证域深度 16 级合法，17 级被拒绝。

## 浏览器走查

1. 左侧默认展开递归树至孙域；域、层、面包屑、子域桶与分流轨迹支持点击下钻和向上定位。
2. 初始展示参数层的直接实验占60%，两个子域占30%，共用0–100%桶轴；孙域全局8%，只继承卡片和购物车两个参数。
3. 初始配置的 `user_219` 经过4个域，执行导航、卡片、排序与交易4项实验；`user_98` 的界面隔离分支执行1项，同时执行祖先排序、交易层，共3项；`user_5` 根级全链路隔离只执行1项。预设按当前拓扑验证目标域，必要时搜索替代用户，缺失分支则禁用。
4. 新建子域：展示父层只余10%空闲时填写11%被拦截；5%成功分配[90%,95%)，折算全局4%，按3+2参数创建两个子层。
5. 在新子层中再次创建占50%的非重叠孙域，折算全局2%；刷新后两级自建域均保留。孙域继承的3个参数不扩展为全局13个。
6. 在新增孙域创建10%实验，预览及详情均显示全局0.20%、A/B各0.10%、完整8节点域层路径；创建 → 提交审核 → 演示启动 → 刷新持久化通过。没有伪造曝光或收益。
7. 原有搜索、筛选、参数JSON校验、版本权重校验、状态流转与CSV导出前一轮走查通过；本轮创建与详情复核递归路径和流量口径。
8. 本轮对1280px测试页面及859px用户页面进行视觉检查，无页面横向溢出；临时视口设置已恢复。原有工作台曾在390px验证导航折叠，本轮不将其作为递归视图的390px验收结果。

新增域与实验的写入验证在127.0.0.1测试数据空间完成；用户交付页使用localhost。两者是浏览器不同origin，验证记录没有写入用户交付空间。

## 限制

本记录不证明生产分流安全、统计推断正确性、身份和权限隔离或跨语言SDK一致性。原型仅实现浏览器内固定user_id的递归规则、模拟创建与本地持久化。服务端分流、共享对照和发布层执行仍按架构方案后续实施。

## 2026-09-12：域下独立新建层

- `npm run build` 通过（TypeScript + Vite）；`npm test` 29/29 通过。
- 新增9项回归：同域多来源参数转移、完整分区、500个用户已有实验分配保持、嵌套域流量继承、未结束实验与子域依赖保护、根域及非重叠祖先约束、名称与标识冲突、参数越界／重复／搬空、星号作用域展开、保存刷新后历史不变。
- 浏览器已验证：常规重叠域显示“新建层”；表单列出来源层与具体禁用原因；ranking.recall可选、运行及草稿参数禁选；空名称与同域重名均被拦截。
- 在127.0.0.1测试空间创建“召回参数层验证”，保存后定位新层，刷新仍可见；继承80%全局名义流量。非重叠域的按钮禁用且就地说明单参数层限制。
- 用户localhost空间仅展示入口，不写入测试层或改动其参数分区。

## 2026-09-12：参数、层与实验关系中心

- `npm test` 38/38 通过；其中新增 9 项关系测试覆盖继承归属、互斥域分区、精确引用、依赖保护、异常归属、历史迁移、结构化及 falsy 值、无效 JSON 和缺失参数。
- `npm run build` 通过（TypeScript + Vite）。构建仍提示主 JS 包超过 500 kB；本轮构建约 742 kB，未做代码分包。
- localhost 页面浏览器验证：导航可进入参数中心；ui.layout 显示 5 个域中的归属和 4 个实验；筛选界面探索子域后显示 1 处归属、1 个实验；点击“管理归属”准确定位该嵌套域。
- 域内参数链接可返回参数中心，关联实验打开正确详情；无匹配搜索显示空状态且可清空恢复；ranking.recall 显示已结束历史引用与允许建层转移的状态。未修改用户的实验或层域配置。
- 859 px 当前浏览器视口视觉检查通过，布局按窄屏换行。关系唯一性、转移限制和校验结果均来自当前本地配置；数据库约束、并发修订检查、审批快照、类型／范围维护和生产 SDK 仍待实现。

## 2026-09-12：注册参数与实验引用

- `npm test` 53/53 通过；新增 15 组注册与集成测试，覆盖递归完整归层、类型与有限 JSON 默认值、目录唯一性、旧配置恢复、100 用户分配完全不变、单快照持久化、存储失败无部分提交、注册定义不可静默修改／删除、五种类型的关系查询。
- `npm run build` 通过；主 JS 约 759 kB，仍有超过 500 kB 的分包提示。
- 浏览器在 127.0.0.1 独立测试数据空间验证：空必填被拦截；选择展示层后递归出现子域，选择内容层后再出现孙域；完成 7 个受影响域的归属选择。number 默认值误填字符串被拦截，改为 20 后注册成功。
- 注册 ui.card_density_check 后，目录由 13 增至 14 项；定义、负责人、默认值和 7 处归属显示正确，刷新后保留。
- 在卡片样式孙层创建测试实验，“添加本层参数”将默认值 20 补到 A/B 两组；B 组填写字符串 "30" 被类型检查拦截，改为数字 30 后通过。重复添加不覆盖 B 组已填的 30；保存草稿后参数中心展示该实验与 A=20、B=30，引用检查通过。
- localhost 用户空间未写入测试参数或实验，只打开空白注册表单。859 px 当前浏览器视口检查表单布局，弹窗内部可滚动，底部操作保持可见。
- 新注册参数只验证顶层类型与可序列化 JSON（最大 32 层），未实现范围／对象字段 schema、参数定义编辑、生产审批或 SDK 默认值下发。模拟器继续仅返回实验参数覆盖，不将全部目录默认值混入结果。

## 2026-09-12：多端、多参数联动契约

- `npm test` 62/62 通过，新增 9 组测试覆盖执行绑定完整性、同参数多端读取、逐组依赖与严格类型／结构等值、跨层拒绝、普通层跨端允许、候选层继承范围、250 用户整组分配不变，以及无效运行契约在配置提交和模拟执行前阻断。
- `npm run build` 通过；主 JS 约 776 kB，仍有超过 500 kB 的分包提示。
- 浏览器在 127.0.0.1 测试空间创建 EXP-1208“三端参数联动契约验证”：在展示层填写 ui.layout、ranking.model、checkout.parallel，页面正确只推荐全链路非重叠域／全参数隔离层；采用候选后 A/B 值完整保留。
- 开启多端契约后，空系统／负责人阻止继续；填写前端、后端、算法 3 个执行绑定、参数消费与逐组实现标识后，未覆盖提示清除。
- 故意要求 compact 布局使用 rank_v3，而 B 组配置 rank_v2，页面明确报“实验组 B 违反联动规则”；修正要求值后可以保存草稿。
- 刷新后“实验配置”保留 3 端实现矩阵与依赖规则，1280 px 视口视觉检查通过；SDK 展示为统一决策透传的待实现示例。用户 localhost 配置未写入测试实验。
- 本地门禁不证明工件存在、已部署、不可变或兼容；没有真实多端 SDK、运行时事务、生产审批或因子统计。配置检查与随机分桶正确不证明业务效果无交互。

## 2026-09-12：应用管理、服务归属与多标签（v8）

### 自动化与构建

- 最终 `npm test`：79/79 通过，0 失败。新增服务目录、环境、标签及独立参数组合规则测试；继续执行全部层域、关系、注册与历史联动回归。
- 覆盖旧目录水合与历史自定义参数待归属、新参数必须绑定服务、跨服务标签与去重、标签不改变实验声明/值/模拟分流、未知引用与重复项、网关委托的环境隔离及环检查、存储失败无部分提交，以及防止省略已有目录造成重置。
- 独立组合规则无需端绑定；逐组条件检查、严格 JSON 类型、缺少参数拦截；删除全部可选规则后空规则集合不会额外阻止 A/A 配置。旧联动契约仍检查完整参数覆盖。
- 最终 `npm run build` 通过（TypeScript + Vite）。主 JS 828.00 kB，gzip 240.49 kB；仍有超过 500 kB 的构建体积提示，未进行代码分包。

### 浏览器验收

写入测试使用 `127.0.0.1:5173` 独立 origin。用户的 `localhost:5173` 仅查看与导航，没有写入测试服务、参数或实验。

1. 应用目录登记网关 `experiment-bff-check`。用户界面服务的测试环境改为委托该网关，开发/生产保持直接决策；刷新及后续修改后仍保留。三环境均为待验证，没有手动伪造连接状态。
2. 从应用详情“参数”页签注册 `ui.result_limit_v8`，自动带入 ui-web 及负责人。类型 number、默认值 20，同时勾选“首页推荐”“性能优化”并创建“联动验收”标签；分配到排序参数层后成功保存。参数服务归属不强制等同参数层类别。
3. 参数目录按标签查看“首页推荐”，显示多个应用的参数。跨应用勾选 ui.layout 与 ranking.model 批量添加“联动验收”；在列表内切换查看参数不会清空标签筛选与批量勾选。
4. 新建“服务标签联动验收”不再选择实验端。排序层按“联动验收”查找时，ui.layout 因越层禁选；ranking.model 与 ui.result_limit_v8 可组成同层跨应用实验。新增参数默认值补入 A/B，保留已有取值，摘要自动展示两应用及负责人。
5. 配置 `ranking.model = "rank_v2" → ui.result_limit_v8 = 30`：B 组填 20 时阻止继续并显示具体规则；改为 30 后创建 EXP-1209。刷新后参数集合、A=20/B=30、组合规则与应用归属均保留。
6. 从 ui.result_limit_v8 移除“联动验收”标签后，EXP-1209 仍明确引用该参数，标签不动态改变实验声明。
7. 参数注册表单内登记 `inline-service-check` 后自动选中该服务与负责人，已经填写的参数 Key 和名称保留；服务目录立即可用，无须跨标签页刷新。该参数草稿未提交。
8. 合并后的唯一一级入口为“应用管理”：应用目录、全部参数两个视图；应用详情保留五页签。应用参数可跳到 `#applications/parameters/<key>`，刷新后仍显示正确参数。旧用户链接 `#parameters/ui.layout` 自动兼容为新路径。
9. 不存在的参数深链接显示“未找到参数”及返回入口，不会回退展示并编辑另一个真实参数。
10. 1280px 验收页与约859px用户窗口进行视觉检查；应用目录与全部参数视图正常排布。当前用户窗口停留在应用目录，保留原20项实验、13项参数。

### 本轮边界

应用登记、接入模式、标签、参数和规则均为本地原型能力。没有真实SDK、凭证、网络探测、曝光回传、RBAC或服务端审批；没有跨标签页并发修订保护。组合规则当前属于具体实验，尚未实现项目级规则模板。参数默认值只用于编写初值，没有生产快照编译与原子下发。本轮验收不代表这些生产能力已完成。


## 2026-09-12：参数归入服务详情维护（v9）

### 自动化与构建

- `npm test`：88/88 通过，0 失败。新增 9 项应用路由测试，覆盖旧入口、实际服务归属、错误服务深链、未归属与未知 Key、认领后的路由变化、编码和幂等性、导航不改变目录或模拟分流。
- 保留并通过现有注册、服务目录、标签、组合规则、递归层域与历史配置回归。固定服务注册与批量提交前的服务范围复验经过代码审查。
- `npm run build`：TypeScript 与 Vite 通过，仍有既有的大于 500 kB 包体积提示。

### 浏览器验收

写入均在 `127.0.0.1:5173` 测试 origin；用户 `localhost:5173` 仅查看与导航，仍为 20 项实验、13 项参数、4 个应用。

1. 应用管理只保留应用目录。进入用户界面服务的参数页，显示本服务参数、标签／层筛选、平铺／标签分组、注册按钮与完整关系，没有全局“全部参数”入口或所属应用切换器。
2. 旧 `#parameters/ranking.model` 自动定位 `#applications/services/ranking-service/parameters/ranking.model`。显式写成 ui-web 下的 ranking.model 也定位真实所属服务，页面标题、选中参数页签和列表同步切换。
3. 旧 `#applications/parameters` 返回应用目录；未知 Key 在目录内显示“未找到参数”，无真实参数详情回退。无效服务 ID 显示“未找到该应用”。
4. 历史 `ui.card_density_check` 从目录内认领为 ui-web 后，立即进入该服务参数详情，仍显示原有 EXP-1207 引用和 A=20/B=30。认领入口仅在存在未归属历史参数时出现。
5. 从服务参数页注册 `ui.service_scope_v9`：所属服务显示只读 ui-web，没有切换或行内登记服务入口，负责人预填前端体验组。number 默认值 20、首页推荐标签、排序层归属提交成功；保存及刷新后定位本服务新参数，参数数量更新。
6. 在 ui-web 中批量选择 ui.card_density_check 和 ui.service_scope_v9，添加“联动验收”成功。按该标签分组只显示当前服务的 3 个匹配参数，切换详情保留筛选；切换到 ranking-service 后勾选归零，目录只有其 4 个参数。
7. 五个应用页签分别刷新检查，均恢复正确页签；新参数详情刷新后保留服务、定义、默认值和标签。
8. 1280px 测试窗口及约859px用户窗口视觉检查通过。用户窗口停留在 ui-web 参数页；嵌入后的重复规则说明已收起，保留注册和维护操作。

### 范围与未重复执行项

本轮改变导航与维护范围，没有变更服务所有权、标签语义、层域分流或实验固定 Key 模型。创建实验的跨服务 ParameterPicker 保留原实现，本轮通过代码审查和相关自动化回归确认，未重复完整实验创建／发布流程。参数表单取消时不注册参数由代码路径确认；主动创建项目标签仍会即时保存标签。真实 SDK、凭证、RBAC、服务端审批和跨浏览器标签页并发保护仍未实现，服务范围限制是当前原型的操作范围，不代表生产权限隔离。


## 2026-09-12：应用私有标签（v10）

### 自动化与构建

- 最终 `npm test`：100/100 通过，0 失败。服务目录测试增至 25 项，保留全部原有层域、关系、注册、组合约束和路由回归。
- 专项覆盖：应用内名称唯一／不同应用同名独立；注册及 add/remove/replace 拒绝跨应用标签；混合应用批量操作被拒绝；直接保存不能迁移已有标签应用归属。
- 旧共享目录按实际使用应用拆分，稳定 ID 和哈希冲突处理、名称／颜色／关联保留、数组重排与重复迁移幂等、精确旧 schema 识别均有测试。现代越界格式不会被当作旧数据修复。
- 隐藏归档保留未使用旧标签；未归属参数通过 pendingTagIds 保留关联。认领复用目标应用同名标签，保存及刷新后仍保留；完成认领后可以正常删除标签绑定，不会持续锁定。
- 迁移写入失败、认领写入失败、坏 JSON 和现代越界配置保持原始存储；失败不会部分提交，加载失败后保存被阻止。App 对加载错误显示恢复页，同时跳过实验持久化，避免把失败加载结果覆盖原数据；此页面分支经代码审查，未在浏览器手动注入故障。
- 最终 `npm run build`：TypeScript 与 Vite 通过。主 JS 836.94 kB，gzip 243.72 kB；保留既有大于500 kB的包体积提示。

### 浏览器验收

新增标签与测试选择均在 `127.0.0.1:5173` 测试 origin 执行；用户 `localhost:5173` 仅读取迁移后的页面，没有添加测试标签或实验。

1. 测试旧目录自动迁移后，ui-web 保留原参数的首页推荐、联动验收等关联；ranking-service 仅展示其实际使用的私有副本，不展示交易体验。原 26 项实验、16 项参数和 6 个应用保留。
2. ui-web 创建“应用私有验收”并绑定 ui.layout；进入 ranking-service 时看不到此标签。在 ranking-service 创建同名标签成功并绑定 ranking.model；再次在同一应用创建同名标签被拦截，提示应用内名称不可重复。后续页面刷新仍保留绑定。
3. 没有标签的 inline-service-check 显示本应用标签空态，批量下拉没有其他应用标签。打开注册参数表单，所属应用固定，标签选择仅显示空态；取消后未创建新参数。
4. 创建实验未筛选应用时，标签区提示先选择应用，参数行不显示其他应用的标签名称。展示参数层内越层参数仍禁选。
5. 切到全链路非重叠域后，先在 ui-web 按“首页推荐”选择 ui.content_card，再切到 ranking-service：标签筛选清空，但待加入项保留。按排序服务的“应用私有验收”仅匹配 ranking.model；选择后显示“2 个待加入参数 · 2 个应用”。批量加入后 A/B JSON 同时包含 ui.content_card 与 ranking.model，涉及应用自动汇总。此流程只验证选参，最终关闭草稿，没有新建或发布实验。
6. 用户窗口的 ui-web 现在只显示其自己的“首页推荐”“交易体验”，没有其他服务的性能优化标签；保留 20 项实验、13 个参数、4 个应用。约859px最终页面视觉检查通过，当前停留在 ui-web 参数页。

### 本轮边界

标签私有范围由本地数据模型、读取助手、写入校验和界面共同约束；这不等同生产身份鉴权或服务端 RBAC。未引用旧共享标签保存在隐藏归档，不自动分配给所有应用。现代格式读取错误需要修复原配置后刷新，不会用演示数据覆盖。未新增真实 SDK、服务端权限、自动发布或跨标签页并发保护；实验仍保存明确 Key 集合，标签不改变已有实验值、层域结构或分桶。

## 2026-09-12：解除参数注册与建层的循环依赖（v11）

### 自动化与构建

- 最终 `npm test`：115/115 通过，0 失败。新增 15 项生命周期专项；旧注册测试改为显式登记与归层两阶段，原有私有标签、递归域、实验关系和路由回归全部保留。
- 覆盖注册不改变作用域／模拟分配、先建空层再归层、缺失与非法选择、深层强制路径与旁支补全、多参数首次激活、未选中星号兄弟层冻结、空层 A/A／子域／容量／并发限制，以及旧无字段数据载入和刷新。
- 保存过渡覆盖已激活参数退回 pending、清空／删除旧非空层、受保护参数转移和写入失败。独立审查发现候选遗漏 `pendingParameterKeys` 可能在全星号配置隐式激活参数，已修正并增加回归：拒绝后原始字节、内存快照和通知数不变，显式完整归层仍正常。
- 最终 `npm run build`：TypeScript 与 Vite 通过。主 JS 850.96 kB，gzip 247.02 kB；保留既有大于 500 kB 的包体积提示。

### 浏览器验收

测试数据仅写入 `127.0.0.1:5173`，用户 `localhost:5173` 只查看页面与打开空白注册表单，仍为原有 20 项实验，ui-web 的 5 个参数保持原值及私有标签。

1. 在 ui-web 下注册 `ui.lifecycle_v11`（number，默认值 20），表单不包含层选择。保存后显示“待归层”、0 处层域归属及独立“分配到层”按钮；根域和全参数隔离层仍只有原 16 个有效参数，没有自动包含新 Key。
2. 在常规重叠域不勾选任何参数，成功创建“生命周期空层验收”。回到参数详情选择此空层，根域和全链路非重叠域自动选择唯一层；3/3 个域齐全后保存，状态变为已归层，显示对应 3 处归属。
3. 再登记 `ui.nested_lifecycle_v11`，在界面探索子域新建“嵌套生命周期新层”并勾选该待归层 Key。默认层、展示参数层、目标新层固定；另一个“验证嵌套子域”未选择时仅 5/6 个域齐全，保存禁用。选择其参数子层 A 后递归加入隔离孙域，7/7 个域齐全才可联合创建。保存并重新进入详情后仍显示已归层及 7 处归属。
4. 登记但不分配 `ui.pending_guard_v11`。实验选参中该 Key 显示禁用状态及“待归层，分配后可用于实验”；其仍未进入根域有效参数摘要。选择已经配置的“生命周期空层验收”后，A/B 自动带入 `ui.lifecycle_v11:20`；修改 B 为 30，成功保存 EXP-1210 草稿，测试实验数从 26 变为 27。
5. UI 空层限制完成后再次创建“待配置限制验收层”：树、详情和创建成功提示均显示待配置，新建子域按钮禁用，提示从应用参数详情分配；模拟器不将此空层列为分流决策。
6. 在 1280px 窗口检查递归归层表单和空层详情，在约 859px 用户窗口检查注册表单，表单可滚动且底部操作可见。用户窗口展示的注册表单中明确标注“注册时无需选择层”。

### 本轮边界

本轮仍为本地交互原型。待归层与历史待归属服务是不同状态，应用私有标签保持原有约束。首次归层按全部继承分支一次保存；已归层参数不通过此入口退回或迁移。未新增生产 SDK 下发、服务端权限、审批或跨标签页并发修订能力。未在用户 origin 创建任何验收参数、层或实验。

## 2026-09-12：受众条件参与层域分流与桶复用（v12）

### 自动化与构建

- 根代理最终独立运行 `npm test`：132/132 通过，0 失败、0 跳过。新增 17 项受众专项，保留全部应用私有标签、参数生命周期、递归层域、实验关系与路由回归。
- 覆盖固定资格画像的三值判定、非法及矛盾条件、同桶实验／子域组合、全部相交区间逐对检查、区间并集占用、针对候选受众的可用容量与首段分配、祖先条件继承、缺属性及不匹配时不换桶、同桶唯一执行及原版本哈希稳定。
- 覆盖受众版本不可变、未知引用阻断、历史配置兼容、完整保存与刷新、写入失败不部分提交、不同系列重名阻断，以及禁止通过更换域／层父节点间接改变既有受众资格。
- 根代理最终独立运行 `npm run build`：TypeScript 与 Vite 通过。主 JS 888.25 kB，gzip 258.08 kB；保留既有大于 500 kB 的包体积提示。
- 独立审查发现并修正父节点迁移绕过受众锁定、非法资格快照静默清空、天数字段小数，以及不同受众系列同名造成选择歧义。模拟器保存错误时停止执行；规则核心与拓扑保存路径的回归已通过。

### 浏览器验收

验收写入仅在 `127.0.0.1:5173` 测试 origin；用户 `localhost:5173` 仅查看和导航，没有添加验收受众、实验或子域。

1. 受众管理显示可编辑规则版本目录、互斥比较及引用关系。新注册用户（入组时注册天数 ≤ 7）与老用户（≥ 8）显示已证明互斥。新建“中国用户验收” v1，国家条件 `IN ["CN"]`，保存成功。
2. 在“生命周期空层验收”创建 EXP-1211“新用户同桶复用验收”，选新注册用户 v1，名义流量 100%、桶 `[0%, 100%)`，A/B 的 `ui.lifecycle_v11` 分别为 20/30。完成创建、提交审核、演示审核通过并启动。
3. 同层创建 EXP-1212“老用户同桶复用验收”：未选择受众时可用容量为 0%；选择老用户 v1 后可用与可复用容量均为 100%，分配相同 `[0%, 100%)`，A/B 值为 20/40。创建、审核预留、启动均成功。图上分行显示两个完整区间，名义配置合计 200%，桶坐标联合覆盖为 100%。
4. 固定 `user_219` 的首次画像为国家 CN、注册 3 天，只执行新用户实验；固定 `user_98` 为 CN、注册 30 天，只执行老用户实验。其他并行参数层继续执行。缺少注册天数的 `missing_audience_v12` 对两个同桶候选均显示资格未知及具体缺属性原因，不执行这两个实验，不改投其他桶，其他层仍执行。
5. 创建实验时展开手动场景估算：基数 1,000,000、全局名义份额 80%、完整条件交集合格率 20%，显示名义人数 800,000、预计合格人数 160,000；实际曝光明确显示“未接入”。该结果是手动假设，不冒充真实人群规模。
6. 在“召回参数层验证”创建“中国人群子域验收”，非重叠子域、占父层 100%、桶 `[0%, 100%)`，绑定中国用户验收 v1。详情展示全局名义流量 80%、自身版本、含祖先的有效条件、继承参数和单一完整参数层。
7. 给“中国用户验收”创建 v2，保留 CN 并追加入组时会员为 true；新旧两个版本均保存。重新打开子域仍引用 v1，仅要求 CN，没有被新版本追加的会员条件改变。
8. 页面重新加载后，`user_219` 恢复首次 CN、注册 3 天画像，字段只读；没有会员属性仍可进入上述 v1 子域。新模拟 trace 列出同桶全部候选：新用户实验匹配、老用户实验不匹配及 `注册天数 ≥ 8` 原因，继续执行相同新用户 B 组。
9. 1280px 窗口对受众、实验创建、条件容量与分流模拟器进行交互及视觉检查。约 859px 用户窗口的受众管理页面视觉检查通过，已打开该页，仍显示原有 20 项实验和 5 个内置受众版本；测试 origin 最终有 29 项实验，测试数据独立于用户工作区。

### 本轮边界

本轮实现本地规则配置、不可变版本引用、资格感知的桶预留与模拟。画像来自手动输入，模拟器在本浏览器按 user_id 保存首次资格快照；纯分流函数仍由调用方提供画像，不代表生产身份统一与资格快照服务已完成。条件只支持受限字段的 AND 规则；不能证明互斥时保守分开占桶。没有真实画像接入、人群规模计算、曝光管道、SDK 下发、生产审批、RBAC 或跨标签页事务保护；手动合格人数估算和名义流量不等于实测曝光。坏快照及写入失败的界面停止路径经过代码审查，未在浏览器手动破坏缓存注入故障。

## 2026-09-12：画像属性与嵌套条件编辑器（v13）

### 自动化与构建

- 根代理在最终源码上独立运行 `npm test`：149/149 通过，0 失败、0 跳过，比 v12 新增 17 项测试。保留全部参数生命周期、应用私有标签、递归层域及固定分桶回归。
- 新增覆盖：完整 AND／OR 树求交、祖先条件、三值逻辑、非法 OR 分支不得被真分支掩盖、空组与复杂度限制、自定义属性类型与枚举值、不可变目录与旧数据恢复、嵌套引用和深链接解析。互斥证明遍历所有分支组合，不能证明时保守阻止桶复用。
- 独立审查修正了非法规则被 OR 真分支掩盖、空 OR 的互斥结论、未知属性类型等边界；抽样检查有效条件树的互斥结论和共同满足示例，未发现错误证明。新增定义的 3 层限制不误用于合成的祖先有效条件，原有 16 层域深度能力保留。
- 最终 `npm run build` 通过（TypeScript + Vite）。JS 931.84 kB，gzip 269.82 kB；CSS 201.84 kB，gzip 39.41 kB。保留既有超过 500 kB 的构建体积提示，尚未代码分包。

### 浏览器验收

写入仅使用 `127.0.0.1:5173` 测试 origin；用户 `localhost:5173` 仅查看页面与打开空白受众表单，仍保留 20 项实验、5 个内置受众版本及 6 个内置画像属性。

1. 在“受众管理 → 画像属性”登记 `customer.loyalty_v13`：名称“客户等级验收”，枚举 silver／gold／platinum，来源用户界面服务，并填写首次入组口径。登记后目录从 6 增至 7 项；刷新属性深链接后元数据保留。搜索 customer 后切换两个页签，搜索和选择保留。
2. 创建“嵌套条件组验收” v1：`country=CN AND (registration_days<=7 OR (customer.loyalty_v13=gold AND is_member=true))`。4 条叶条件、3 层条件组；第 3 层继续添加组的按钮禁用。属性选择器显示类型、Key、来源和口径，运算符及值控件跟随类型。
3. 手动资格预览：CN、注册 3 天时，即使缺少等级和会员字段仍匹配；改为 30 天时显示未知及具体缺属性原因；补 gold 与会员 true 后匹配。预览明确是临时手动判断，不保存用户、不推测真实人数。
4. 与“老用户 v1”比较显示潜在重叠，构造示例 CN、注册 8 天、gold、会员 true；没有仅凭新用户分支就误判互斥。示例明确标注由规则构造，不代表真实用户或人数。
5. 在中国人群子域验收的完整参数层创建 EXP-1213“嵌套画像实验验收”，绑定上述受众、流量 100%。摘要与详情显示 CN 祖先条件与完整嵌套树的 AND 关系。创建、提交审核、演示启动成功，桶为 `[0%, 100%)`。
6. 第二个实验内打开“新建受众”再取消，原子域选择和 100% 流量保留。再次快捷创建“老用户银卡验收”，条件为注册天数 ≥ 8 且等级 silver；祖先 CN 只读显示，保存后自动选中，原实验名称与参数保留。可用容量从 0% 变为 100%，显示复用同一 `[0%, 100%)`。EXP-1214“互斥条件组实验验收”创建、审核、演示启动成功；桶图分行显示两组 100%，联合覆盖仍为 100%。
7. 模拟器恢复历史 user_219 的 CN、注册 3 天固定画像，新属性不允许补填；仅命中 EXP-1213。user_98 的 CN、注册 30 天快照缺少必要等级／会员字段，两个实验均资格未知、不执行，其他并行参数层仍执行。
8. 新用户 audience_v13_silver1 提供 CN、注册 30 天、silver，只有 EXP-1214 命中实验组 B。刷新后重新输入该 ID，silver 恢复且不可修改，分流仍为相同 B 组，没有因刷新或新增目录改写首次资格。
9. 在召回参数层新建子域时快捷创建“海外金卡人群验收”：US 且 gold。保存后自动选择，父表单的“海外金卡子域验收”名称和 100% 流量保留；与已有 CN 子域证明互斥，可复用 100% 桶。子域创建成功，详情显示完整条件和固定受众版本。
10. 属性引用详情最终列出 3 个受众版本、海外金卡子域及 EXP-1213／1214 两个实验，嵌套条件中的属性没有漏记。测试 origin 最终有 31 个实验、10 个受众版本、7 个画像属性，刷新及后续写入均保留。
11. 1280px 测试页检查属性详情、嵌套抽屉、实验配置与模拟器；约 859px 用户窗口检查属性页和新建受众抽屉，布局可用、底部保存／取消按钮可见。用户窗口已打开空白新版受众编辑器，没有保存测试配置。

### 本轮边界

- 属性支持枚举、数值、布尔、文本；自定义属性绑定已登记应用，保存后不可原地修改或删除。内置枚举保留开放值兼容，自定义枚举严格使用登记值。属性属于画像目录，不参与实验参数归层。
- 条件编辑最多 3 层、30 条叶条件；互斥证明最多展开 64 个组合，超过上限保留完整执行表达式，但不给出已证明互斥的结论。旧 AND 规则和受众版本固定引用继续兼容。
- 当前交付是浏览器本地配置、规则校验和模拟。真实画像采集、人群规模、身份统一、生产 SDK、曝光、RBAC、服务端审批及并发发布事务均未接入。动态自定义快照的缓存故障路径通过代码审查和核心持久化回归确认，未在浏览器破坏用户缓存进行故障注入。

## 2026-09-12：画像属性简化、创建时检查与引用导航（v14）

### 自动化与构建

- 根代理独立运行 `npm test`：172/172 通过，0 失败、0 跳过。相较 v13 新增 23 项：3 项来源解绑及枚举兼容、13 项创建预检、7 项引用路由及实际关系核对。
- 预检覆盖完整 OR 比较与见证、条件未完成不产生结论、普通重叠允许中央登记、最新父层祖先解析、继承矛盾、实验／子域预留、连续桶碎片、互斥复用、复杂度未知、错误上下文与提交前重新检查。预检不修改原始配置，保存受众不预占流量。
- 引用导航覆盖受众版本／属性精确标识、旧实验名称兼容、直接及祖先继承、来源不相关或不存在、无效／重复查询参数、安全编码，以及普通目标路径兼容。
- 根代理在最终源码上独立运行 `npm run build`：TypeScript + Vite 通过；JS 945.76 kB，gzip 274.07 kB，CSS 210.26 kB，gzip 41.02 kB。保留既有超过 500 kB 的包体积提示。
- 来源解绑、预检、保存门禁与路由接线均完成交叉只读审查，未留阻断问题。移除来源后，自定义枚举仍按登记值闭合，不能因缺省 serviceId 被误认为内置开放枚举。

### 浏览器验收

测试写入仅在 `127.0.0.1:5173`。用户 `localhost:5173` 保留原有 20 个实验、5 个内置受众版本和 6 个内置属性；仅打开已有受众的新版本编辑器查看预检，没有保存。

1. 属性登记、筛选、列表、详情和条件选择器均移除来源应用。登记 `customer.segment_v14`（“无来源属性验收”，枚举 standard／premium）只填写 Key、名称、取值及统计口径，保存成功；刷新后仍显示允许值，无需应用选择。历史 `customer.loyalty_v13` 保留原定义和引用。
2. 创建“创建前自动检查验收”，条件为 segment=premium。条件未完成时保存禁用；填写完整后自动比较 10 个已有受众版本并展示关系，无需保存后另开比较器。普通潜在重叠仅提示，允许登记。
3. 同一 AND 组追加 segment=standard，立即提示条件分支矛盾、受众交集为空，保存禁用。删除矛盾条件后检查恢复通过并保存，新版本固定到 `audience-dd6303e7-2af`。
4. 在中国人群子域的参数层内快捷创建受众，父表单名称“内联检查父草稿验收”、流量 10%。尚未填写时即显示祖先 CN 条件。填写 US 后提示与祖先交集为空，阻止“保存并选用”；改为 CN 后在创建受众时显示两个运行实验的完整条件及 `[0%,100%)` 预留，提示无连续 10% 桶段，继续阻止保存。
5. 将该草稿改为 CN 且注册天数 ≥ 8 且等级 platinum，与既有“新用户或金卡会员”“老用户银卡”完整条件互斥。创建前检查恢复通过，可用／最大连续区间均为 100%，按钮可用；保存“老用户白金卡创建预检验收”后父表单自动选中，原名称与 10% 流量保留。随后取消父表单，没有创建额外域或实验。测试空间最终 31 个实验、12 个受众版本、8 个画像属性。
6. 从嵌套受众的引用条目进入 EXP-1213，链接携带固定受众 ID，自动打开“实验配置”并将焦点定位至绿色受众条件区域；展示完整嵌套表达式、直接引用及包含祖先的有效条件。刷新后页签和定位恢复。
7. 从 country 属性进入 EXP-1214，自动显示“祖先继承”、来源“中国人群子域验收”及 CN 相关规则；从同一属性进入海外金卡子域，准确定位该域，显示 US 规则及完整条件，刷新仍保持。来源条目本身也展示各目标的有效条件。
8. 使用未被 EXP-1213 引用的 segment 属性上下文打开该实验，显示未引用提示，保留真实实验及概览页，没有伪造关联或应用额外条件。
9. 1280px 检查引用定位面板；约 859px 用户窗口打开“新注册用户”的新版本编辑器并定位“创建前检查”，显示 1 个互斥、4 个可能重叠，检查通过；内容可滚动、底部操作可见。未提交新版本。

### 范围与兼容

新画像属性不接收或写入来源应用；历史来源字段仅原样保留，不要求对应服务仍存在。当前受众条件和版本不改写。实验内的预检上下文透传与提交门禁经源代码审查、共享纯函数回归确认；本轮浏览器完整内联阻断／修正流程使用子域入口，实验创建全流程保留 v13 验收记录。

中央受众登记不绑定桶位置；重叠本身合法。内联创建结合当前层和流量检查是否可用，父表单最终保存仍复验并分配桶。导航 URL 只携带资源标识用于解释引用，不携带可执行的条件覆盖。以上均为本地原型能力，没有新增画像查询、服务端事务、跨标签页并发控制或生产审批。


## 2026-09-12：受众筛选与实验列表跳转（v15）

### 自动化与构建

- 根代理独立运行 `npm test`：180/180 通过，0 失败、0 跳过。本轮新增 8 项回归，覆盖固定版本筛选、直接与祖先域继承引用、旧受众名称兼容、空集合、未知版本、URL 编码及无效／重复查询参数。
- 最终源码独立运行 `npm run build`：TypeScript + Vite 通过；JS 949.35 kB，gzip 275.25 kB；CSS 212.07 kB，gzip 41.34 kB。保留既有超过 500 kB 的包体积提示。
- 筛选函数、引用入口和列表接线完成交叉只读审查；详情返回仅保存合法的实验列表地址。

### 浏览器验收

本轮全程只读，没有新增或修改实验、域、受众与属性。测试 origin 保持 31 个实验、12 个受众版本、8 个属性；用户 origin 保持 20 个实验、5 个内置受众版本、6 个内置属性。

1. 中国用户验收 v1 的“查看引用实验 2”打开 `#experiments?audience=audience-9152df6a`；实验列表自动选择对应版本，仅展示从祖先域继承该受众的 EXP-1213 和 EXP-1214，没有进入详情定位面板。
2. 刷新后 URL、受众选择和两条结果保留；叠加搜索 `nested_audience_v13` 后仅剩 EXP-1213。进入普通实验详情，再点击应用内“返回实验管理”，保留同一受众筛选及搜索文本。
3. 切换中国用户验收 v2 后显示无引用空态，没有混入 v1 结果；清除受众筛选恢复全部 31 个实验。搜索和受众条件在列表内部切换时组合生效。
4. 列表输入不匹配搜索后，从受众详情重新点击引用入口，残留搜索清除，恢复该受众的两条结果。
5. 访问未知受众版本时显示“不可用受众”及错误提示，列表为空，没有回退展示全部实验。
6. country 属性详情按使用该属性的受众版本分组。点击“嵌套条件组验收 v1”的“查看引用实验 1”进入相应受众筛选列表，只展示 EXP-1213。
7. 约 859px 用户窗口从“全部活跃用户 v1”点击“查看引用实验 20”，打开带对应筛选的实验列表。搜索、应用筛选、受众选择及清除入口均可见，筛选行正常换行；宽表保留原有内部横向滚动。用户页面留在该列表供查看。

### 范围与兼容

实验列表按固定受众版本筛选配置引用，包含直接引用及祖先域继承；这是配置关系查询，不计算真实用户规模。受众与属性详情的新实验入口均指向列表；v14 实验详情定位链接仅作历史兼容，域引用入口保持原行为。本轮验证列表刷新和应用内详情返回，未单独验收浏览器后退／前进按钮。


## 2026-09-12：层域参数入口适配应用参数管理（v16）

### 自动化与构建

- 独立运行 `npm test`：180/180 通过，0 失败、0 跳过。本轮复用既有生命周期、应用私有标签、跨应用首次归层、完整参数分区及原子保存回归；未新增或改写分流、目录、归层核心逻辑。
- 最终源码独立运行 `npm run build`：TypeScript + Vite 通过；JS 954.40 kB，gzip 276.84 kB；CSS 216.78 kB，gzip 42.25 kB。仍有既有单包超过 500 kB 提示。
- 对共享浏览器、层域接线与两种编辑器做交叉只读审查。修正非重叠子域弹窗的参数导航会丢失草稿的问题：该展示保留筛选，关闭应用／参数跳转；层域详情仍提供真实所属应用链接。

### 浏览器验收

1. 交易层最初只有 4 个参数，按交易服务 3 项、优惠策略服务 1 项分组。筛选交易服务及“性能优化”后仅展示 2 项；切换策略服务清除旧标签条件，只展示优惠参数 1 项。“交易体验”仅匹配当前策略应用。选择层内没有参数的用户界面服务后显示空态，不带入层外参数。
2. 在交易层的新建重叠子域草稿中，A 初始 2 项、B 为 2 项。筛选策略应用并勾选 coupon 后，A 为 3 项、B 为 1 项；切回交易服务，已选择的两个交易参数仍勾选。完整父层范围始终是 4 项，隐藏项未被筛选删除。随后取消，没有创建该子域。
3. 常规重叠域的继承范围展示 18 项；新建层候选为 19 项，额外包括已登记但尚未归层的 ui.pending_guard_v11。已有未结束实验、子域继承等禁用原因显示并保留；当前可选 3 项，没有把应用筛选作为新的层归属约束。
4. 仅在 `127.0.0.1:5173` 创建“应用参数关联验收层”（layer-124b016e）。先按用户界面服务选中待归层的 ui.pending_guard_v11，再按交易服务选择可转移的 checkout.inventory，切回应用仍保留选择，汇总为 2 个参数、2 个应用。首次归层预览 3/3 个域完整，新建层与首次归层一次保存；来源交易层仍保留其他参数。
5. 保存定位新层，两个参数按实际应用归属分别展示，待归层参数变为已归层。刷新后新层、两项关联与所属应用保持。按登记名称“待归层选参限制验收”搜索只返回相应参数；点击参数进入用户界面服务下的准确参数详情，该页关系校验通过，层归属链接可返回新层。
6. 新层的非重叠子域草稿填写名称后，继承列表显示两个应用的完整参数。应用与参数均为不可跳转文本；筛选交易服务只改变可见结果，保留子域草稿和当前地址。随后取消，没有创建新子域。
7. 在 1280px 测试窗口和约 859px 用户窗口检查层参数区域。窄窗口的应用选择与搜索正常换行，分组、参数和状态可读，列表内部可滚动。用户 `localhost:5173` 仅打开原有交易层（仍为 4 项参数）展示新界面，没有保存测试配置。

### 范围

域继承范围、层关联参数、新建层和新建子域共用应用参数浏览方式，按 Key、登记名称、应用搜索；只在选定应用后显示其私有标签。标签筛选不改变固定参数 Key、应用归属和分流约束。一个层仍可包含多个应用的参数；新建层保留空层、首次归层和受保护转移的既有流程。测试 origin 本轮仅新增一层并完成所选参数分配，没有新增实验、域、受众或属性。历史应用未归属参数保留独立分组，按原兼容策略处理。


## 2026-09-12：独立分流验证页与关注节点解释（v17）

### 自动化与构建

- 根代理独立运行 `npm test`：193/193 通过，0 失败、0 跳过。新增 13 项专项，覆盖验证路由编码、非法路径／查询、旧链接兼容、未知节点、完整根路径只读解释、到达不等于实验命中、祖先桶外、根域资格拒绝／未知、同桶缺属性与其他候选匹配、空层、缺记录和引擎错误优先。
- 最终源码独立运行 `npm run build`：TypeScript + Vite 通过；JS 964.05 kB，gzip 279.94 kB；CSS 227.88 kB，gzip 44.29 kB。仍保留既有单包超过 500 kB 提示。
- 独立交叉审查路由接线、隐藏页保留、画像快照、模拟时配置解释及焦点目标，未发现阻断缺陷。本轮未修改分流引擎、哈希分桶或参数归层算法。

### 浏览器验收

1. 层域管理出现“层域配置／分流验证”两个子页面。原配置页不再展示画像输入或模拟器；域、层详情均有“验证分流”。从内容深度探索域进入 `#traffic/validation/domain/card-depth`，关注该域，并明确从默认域开始完整分流。
2. 填写模拟 user_id 与 CN 画像，切换配置再回验证，输入完整保留，未自动执行。执行孙域示例 user_219 后，结果显示到达内容深度探索域，完整经过 5 个域、执行 6 个实验，同时包含排序、交易及其他并行层；以上数量为测试 origin 当前配置结果。
3. 从结果的“卡片样式孙层”进入其配置，再切回验证，仍保留 user_219、原关注域、6 个实验／5 个域结果。清除关注只移除关注上下文，不改变完整分流的计算方式。
4. 切换全链路示例 user_5，结果为 1 个实验、2 个域；关注域未到达。解释精确指出首个未到达祖先为常规重叠域，默认层桶 97.7% 在其 [0%, 80%) 区间外；“查看阻断位置配置”准确打开常规重叠域。
5. 仅在 `127.0.0.1:5173` 创建空层“分流验证配置更新验收层”（layer-6d40ca19），不选择或移动参数。切回验证页，user_5 和原结果保留，醒目标记“输入或配置已更改，请重新验证”，关注解释标记为上次配置。点击重新模拟后恢复本次结果，过期提示消失。
6. 从新空层点击“验证分流”，将关注切换至该层但不自动重算。执行 user_219 后解释该层无已归层参数、被跳过，不将空层视为受众失败；完整结果仍为 6 个实验、5 个域。
7. 刷新验证地址保留验证页及关注的 card-depth 域，表单和结果重新初始化，没有自动模拟。历史固定画像仍由原有存储机制读取；本轮未宣称刷新恢复完整结果。
8. 访问不存在的关注层保留其原始 ID 并显示不可用说明，没有替换为真实节点。无效编码的验证路径显示独立地址错误页并隐藏模拟器，提供打开完整验证的入口。
9. 旧域引用 `#traffic/domain/domain-dcac0900?attribute=country&focus=audience` 仍打开配置页并显示已定位的画像属性条件。切到该域验证再返回配置页，原完整查询地址及来源说明保留；验证路由未混入旧受众查询参数。
10. 1280px 测试窗口检查输入／结果双栏、关注卡片、完整路径及合并参数区；约 859px 用户窗口检查上下布局、页签与画像字段，均可读且无横向内容溢出。用户窗口停留在 `localhost:5173/#traffic/validation/layer/transaction`，没有运行模拟或保存测试画像、层域配置。

### 范围与状态保留

两个子页面在层域管理内部保持挂载，隐藏区域不显示、不进入常规焦点顺序。输入、结果及模拟时配置快照保留在当前页面内存；离开层域主模块或刷新会重新初始化，首次资格画像继续使用既有 localStorage，不因布局迁移重置。同一结果始终包含完整默认域分流，关注节点仅作定位和解释。配置更新后的旧结果使用模拟时快照解释并标明过期；并未将关注节点变成新的分流起点。

本轮测试 origin 仅新增一个空层并使用模拟画像存储，没有新增实验、域、参数、受众或属性；用户 origin 原有 20 个实验及参数配置保持。实际验收包括应用内页签、结果返回、地址刷新和旧引用兼容；浏览器后退／前进按钮未单独进行操作验收。

## 2026-09-12：子域自动创建初始参数层（v18）

### 自动化与构建

- 根代理独立运行 `npm test`：205/205 通过，0 失败、0 跳过。新增 12 项专项覆盖两种模式的唯一普通初始层、父范围完整继承、单参数重叠域、后续拆层、非重叠祖先限制、当前桶预留与原实验分配稳定、互斥受众桶复用、空交集／未知受众、空父层、非法输入、碎片容量、待归层参数隔离及失败不修改输入。
- 最终源码独立运行 `npm run build`：TypeScript + Vite 通过；JS 964.73 kB，gzip 280.19 kB；CSS 227.41 kB，gzip 44.20 kB。保留既有单包超过 500 kB 提示。全量测试之后仅调整初始层说明和单参数场景提示，未再修改分流或规划行为。
- 只读交叉审查确认 A/B 名称、参数勾选及旧样式已移除；`planChildDomainCreation` 使用提交时的拓扑重新校验和寻找可用桶段，域与初始层通过同一次 `saveTopology` 保存。已有星号继承与 `planLayerCreation` 的具体剩余参数转换兼容。

### 浏览器验收

1. 在独立测试 origin `127.0.0.1:5173` 的“应用参数关联验收层”（layer-124b016e）打开新建子域。表单仅需填写子域配置，显示“初始参数层／自动创建 · 1 个层”，继承两个应用的两个参数；无 A/B 层名输入或参数分配勾选。
2. 选择“交易服务”及其“交易体验”标签后，只显示 checkout.inventory，但范围仍为 2 项。直接保存“初始参数层流程验收”（domain-cb2b15ba），生成唯一普通初始层（domain-cb2b15ba-initial），完整包含 checkout.inventory 与 ui.pending_guard_v11。自动定位新域并提示初始化结果；刷新后保持一个层、两个参数，筛选未改变继承范围。
3. 从新域点击“新建层”，填写“库存业务参数层”。全选两个参数时显示初始层至少保留一个参数，创建按钮禁用。取消 ui.pending_guard_v11 的选择后保存，创建 layer-366910c7 并只转入 checkout.inventory；初始层保留 ui.pending_guard_v11。刷新后两个层仍各有一个参数，域内完整分区保持。
4. 从只有一个参数的“库存业务参数层”打开新建子域，重叠模式可用且默认选中，初始化仍为一个完整层；随后切换非重叠模式并创建“单参数隔离初始化验收”（domain-41e8e530）。生成一个普通初始层（domain-41e8e530-initial），完整继承 checkout.inventory；域详情的“新建层”禁用并说明非重叠分支约束。单参数重叠的实际保存由专项测试覆盖，浏览器此步骤只保存非重叠域。
5. 约 859px 用户窗口中，原交易参数层仍有 4 项参数，实验管理仍显示 20 项。仅打开创建草稿，检查初始化卡片、应用筛选、参数列表和固定底部按钮可读，已移除两个名称输入框。页面停留在初始化卡片供查看，没有在用户 origin 保存测试域、层或实验。

### 范围

重叠和非重叠子域均自动生成一个“初始参数层”，角色为普通参数层，完整继承当前父范围；重叠模式允许后续按业务新增／拆分，非重叠模式保持单完整层。参数应用归属、应用私有标签、祖先受众交集、连续桶及已有实验依赖保护沿用原规则。已有多层域不迁移或改名。v16 的 A/B 双层创建验收仅为历史记录，现行流程以本节为准。

本轮浏览器测试只在独立测试 origin 新增两个子域及三个参数层（含两个初始层），未新增实验、参数、受众、画像属性或应用。

## 固定 10,000 桶与离散组合分配（v19）

### 自动化、审查与构建

- 根代理最终独立运行 `npm test`：227/227 通过，0 失败、0 跳过。覆盖离散总容量分配、0.01% 单桶粒度与精度拒绝、显式数组完整性、真实多段冲突、受众互斥复用、空洞不命中、后段命中、子域初始化、审核相关预检与旧桶兼容。
- 独立审查发现并修复旧无数组记录的精度兼容问题：历史 `traffic=1.001` 继续按原比较边界执行，新申请仍要求两位小数。新增 300 个用户与等价实际桶集合的入组、版本、参数一致性对照。非法显式数组不允许回退为旧单段。
- 小流量百分比共用自适应显示，0.005% 不再舍入为 0.01%，深层非零比例不显示为 0。创建表单、详情、树、分流轨迹与人数假设说明同步采用该格式。
- 最终 `npm run build`：TypeScript + Vite 通过；JS 975.33 kB，gzip 283.77 kB；CSS 232.83 kB，gzip 45.10 kB。保留既有单包超过 500 kB 提示。

### 浏览器验收

所有保存操作仅在独立测试 origin `127.0.0.1:5173` 完成。

1. 在既有“初始参数层流程验收”域的初始层（domain-cb2b15ba-initial，继承 ui.pending_guard_v11）创建“桶预留占位验收”（EXP-1215），20% 草稿预览 [0, 2000)。提交审核后详情显示保留 2,000 桶，父层分解显示待审预留 2,000、未占用 8,000。
2. 在同层创建 20% 的“桶间隔占位域”（domain-d38ff278），自动分配 [2000, 4000)，并生成完整继承一个参数的普通初始层。将 EXP-1215 退回草稿后，其 2,000 桶释放，形成 [0, 2000) 与 [4000, 10000) 两段空闲位置，合计 80%。
3. 创建“离散桶实验验收”（EXP-1216）。先输入 0.01%，预览 1 桶 [0, 1)，A/B 各占本域 0.005%、全局名义各 0.0004%。再申请 70%，实际预览 [0, 2000) ∪ [4000, 9000)，共 7,000 桶，超过任一单段空闲长度仍可成功创建。
4. EXP-1216 提交审核后刷新，仍保留原两段。演示审核通过启动、暂停、刷新、恢复均保留完整两段；结束后详情标为“历史配置 · 已释放”，父层恢复 8,000 桶可用。未修改其他既有测试实验的状态或分配。
5. 在相同父层创建“离散桶子域验收”。输入 80.01% 提示申请 8,001、可用 8,000，点击保存仍拒绝；输入 0.015% 提示精度无效，不静默舍入。恢复 70% 后保存为 domain-8782a1aa，刷新仍为 [0, 2000) ∪ [4000, 9000)，自动生成一个完整继承参数的初始层。
6. 父层联合占用 9,000 桶，未占用 1,000；分类显示直属子域 9,000、实验与预留均为 0。真实桶图为同一离散域生成两个可下钻段，中间 [2000, 4000) 仍归占位域；配置列表中每个域只有一项，未使用首末边界包络代替真实桶集合。
7. 1280px 测试窗口检查多段子域表单和占用汇总；约 859px 用户窗口检查新建子域的固定桶比例、当前受众可用桶数、实际桶段和固定底部按钮，内容可读。用户 origin 实验仍为 20 项，未保存任何测试实体。页面停留在展示参数层的新建子域草稿与桶分配预览。

### 范围

本轮新增两项测试实验（最终一项草稿、一项已结束）、两个测试子域和两个自动初始层；未新增应用、参数、受众或画像属性。当前创建、审核预留、受众预检、详情和模拟解释统一使用实际多段桶集合。旧配置兼容读取，不批量重排已有桶。新建子域的初始层、参数应用归属、应用私有标签及祖先受众约束继续保留。本节替代历史版本关于“最大连续空闲长度”的创建限制；生产配置发布、SDK 和服务端并发事务仍属于架构方案范围。

## 多分组与组合约束下线（v20）

### 自动化、审查与构建

- 根代理独立运行全量 `npm test`：242/242 通过，0 失败、0 跳过。新增 11 项核心多组专项与 4 项草稿操作专项，验证多组身份与唯一对照、名称和精度校验、全部组的参数完整性、整数边界选择、旧分组不重排、已下线规则不再阻断、均分尾差及多组批量添参不改写已有值。历史规则纯函数测试保留作为归档工具测试，不代表产品运行链仍启用该功能。
- 独立审查发现并修正两处数据风险：切换域／层不再自动覆盖所有组的参数原文；批量添参与格式化先检查有限 JSON，避免 `1e309` 被序列化为 null。重新初始化是显式按钮，可立即撤销；继续编辑组配置后清除旧撤销快照。
- 最终 `npm run build` 通过：JS 979.75 kB，gzip 285.53 kB；CSS 229.90 kB，gzip 44.52 kB。保留既有单包超过 500 kB 提示。全量测试后仅调整格式化状态提示在继续输入时清除，并补充文档；最终构建涵盖该提示改动。

### 浏览器验收

所有保存仅发生在独立测试 origin `127.0.0.1:5173`，用户 origin 未新增或修改实验数据。

1. 创建“多分组配置流程验收”。第二步出现分组列表及单组编辑区，无组合规则入口。选“初始参数层流程验收”域后，原 ui.layout 草稿仍保留并提示超出当前层；显式初始化后，各组改用 ui.pending_guard_v11。
2. 新增第三组 C，名称改为“策略召回组”，参数值设为 strategy_c。复制为 D 后，D 起始参数相同且使用新的分组 ID、流量待分配；修改 D 为 strategy_d，再删除 D，C 仍保留 strategy_c。新增／删除均不自动修改其他组权重。
3. 点击均分流量后，三组为 33.34%、33.33%、33.33%，合计 100%。将第三组设为对照，其他两组转为实验角色、顺序不变；当前对照的删除按钮禁用。将 A 的名称改为“基准参数组”。
4. 在 C 输入包含 1e309 的 JSON 并点击格式化，提示有限 JSON 错误、原文保留；输入错误 JSON 后切到 A，点击下一步仍定位公共错误摘要，并标识隐藏 C 组参数待修正。修复 C 后，将权重设为 0，下一步拒绝并说明正数及精度要求；恢复 33.33% 后可继续。
5. 切换至“库存业务参数层”，C 的 strategy_c 原文保留。显式初始化生成 checkout.inventory 默认参数，点击撤销恢复原各组配置；切回初始层，三组身份、角色、权重和 C 的差异值保持。1280px 窗口检查分组列表、角色、权重与单组参数区可读。
6. 保存为 EXP-1217 后详情显示全部三个分组及待采集数据。提交审核、演示审核通过启动并刷新，仍保持三个稳定 ID；第三组“策略召回组”为对照，权重 33.33%。实验占父层 [9000, 10000) 的 1,000 桶，组权重以这项实验为分母。实验配置页完整保留三个参数 JSON，C 为 strategy_c，A/B 为 default。
7. 新实验详情未显示组合约束，也未借用原两组示例产生第三组样本、提升或结论。CSV 和模拟器的多组身份处理由源码交叉检查及核心专项覆盖，本轮浏览器未额外下载 CSV 或为该嵌套实验穷举用户命中。

### 范围与后续方案

本轮仅新增一项测试实验 EXP-1217，最终运行中；测试 origin 实验数为 34。没有新增应用、参数、层域、受众或画像属性。新建实验支持 2–20 组、一个对照组；旧实验仍按原顺序与哈希解释。已下线的组合规则字段不删除，但不再展示或参与运行校验。

[多分组与批量参数配置方案](../多分组与批量参数配置方案.md) 已交付，覆盖普通参数与嵌套策略 JSON、固定基线与各组覆盖、路径差异、应用私有标签边界、导入预检和完整快照。复杂表格、JSON 树编辑、基线、字段 schema 与批量导入本轮尚未实现。

## 2026-09-12：v21 全页实验草稿工作区

### 实现与自动化

- 新建实验由三步弹窗改为全页工作区：实验目标、范围与流量、分组与参数、检查与提交可自由切换。编辑草稿独立于正式实验，逐项保存在 `exp-lab-experiment-draft-v1:<draftId>`，刷新恢复原文、配置分区及选中的分组；草稿列表显示本机未提交记录。
- 新增 32 项专项（草稿存储／路由／复制 17 项，提交配置校验 15 项）。根代理独立运行全量测试：274/274 通过，0 失败、0 跳过。覆盖未完成字段及坏 JSON 原文、存储读取／写入失败、坏版本记录、乐观 revision 冲突、幂等提交标记、严格路由、复制固定受众与新分组 ID、20 组完整性、隐藏组错误定位元数据、待归层及跨层参数、类型／应用归属、单桶精度和碎片总容量。
- 复制旧名称受众时映射到已知内置固定版本；未知旧受众保留为不可用标识，要求显式修正，不能静默放宽。提交重新读取正式实验、重新分配当前可用桶；检测到其他页面更新层域配置时要求刷新后检查。正式写入成功后才离开工作区，提交标记使用最终实际实验 ID。
- 审查修正：无域／路由层不误报容量不足；待归层状态优先于跨层说明；多个对照组定位角色区；另一页提交后不隐藏当前未保存内容；重复提交恢复使用实际实验 ID。坏数据不以空草稿覆盖，保存失败保留当前编辑及备份操作。
- 最终 TypeScript／Vite 构建通过；主包 998.57 kB（gzip 291.56 kB），CSS 238.64 kB（gzip 46.19 kB）。仍有既有单包超过 500 kB 的体积提示。

### 浏览器验收

所有新增草稿和实验写入均在独立 `127.0.0.1:5173` 来源，未在用户 `localhost` 数据中写入测试记录。

1. 从实验列表“创建实验”进入独立草稿地址，填写名称、标识和核心指标后可直接跳到分组区，无前一步门槛。新增第三组“召回策略 C”，均分为 33.34%／33.33%／33.33%。在 C 中只输入 `{"ui.pending_guard_v11": ` 后直接刷新：未完成 JSON 原文、三个组、选中 C 及所在分区完整恢复。
2. 检查页列出第三组的 JSON 问题；点击后返回分组区，实际焦点位于“分组 C 参数 JSON”。坏 JSON 不被解析成空对象，也不会因切换范围清空原文。修正后，各组相同 Key、不同值能通过检查。
3. 选择“桶间隔占位域／初始参数层”，预览 1,000 桶、本域 10%、全局名义 0.16%；拟分配桶段默认折叠。返回实验列表出现“编辑中的草稿”，正式实验数仍是 34；继续草稿保留全部字段及选中组。1280px 工作区视觉检查完成，导航、主编辑区、保存状态与提交入口可读。
4. 在第二页打开同一草稿，更改 B 参数并保存。第一页停止自动保存，原 B 值仍保留，显示另存／加载最新／导出。点击另存产生新草稿地址，旧 B 原文保留；第二页的更新值未被覆盖。
5. 第二页提交“草稿工作区验收”为 EXP-1218，状态为待审核，分组为三组，预留 `[0,1000)`。刷新后身份、权重及桶位保持，样本和收益仍为待采集。重新打开原草稿地址显示“这份草稿已提交”及 EXP-1218 链接，不重复创建实验。
6. 从既有 EXP-1217 点击“复制为新实验”：名称附副本、实验 Key 留空；保留三个组及第三组对照角色、权重、各组原值、范围和护栏。原父层无剩余容量，复制草稿如实显示容量问题，不排除源实验占用。新 ID 由专项测试验证；浏览器未为该复制稿再次预留流量。
7. 层详情新增“创建实验”链接；“桶间隔占位域／初始参数层”显示待审核预留 1,000 桶、未占用 9,000 桶。点击创建入口，新草稿自动带入域和层；目标字段待填写。
8. 复验“另页提交时保护当前未保存输入”：准备同一草稿两页，第一页切换检查区保存后，第二页进入版本冲突并继续填写本地描述；第一页提交 EXP-1219。第二页仍保留编辑区和未保存描述，可另存为新稿；另存后刷新，描述原文仍在。未把本地内容藏在只读已提交页面后面。

### 范围与限制

本轮测试来源新增正式实验 EXP-1218、EXP-1219，均处于待审核，最终正式实验数 36；另有未提交的流程／复制／冲突备份草稿。不新增应用、参数、层域或受众。初次打开已提交草稿可查看对应实验；正在编辑的页面遇到外部提交则保留输入并提供另存和导出。

自动保存、revision 检查和存储事件是浏览器原型保护，不是跨标签页原子事务或生产多人协作服务。正式实验与草稿标记分开写入，来源草稿标识用于恢复查找与防止顺序重复提交；生产需服务端幂等键及事务。存储失败由专项测试覆盖，浏览器未刻意填满容量或关闭存储权限；未验证真实 SDK、服务端审核、统计结论或跨设备共享。

普通参数表格、固定配置基线、JSON 树、字段 schema 和批量导入继续保留在 [多分组与批量参数配置方案](../多分组与批量参数配置方案.md)，本轮没有宣称实现这些编辑器。

## 2026-09-16：v22 层域管理信息

### 实现与专项验证

- 新建层、新建子域及层域详情共用负责人、长期／临时、预计结束日期字段。负责人默认演示当前用户“陈思远”，保存前去除首尾空格；临时必须填写有效日期，长期清除日期。历史节点未提供管理信息时保持“未设置”。
- 子域的初始参数层在创建时复制一份管理信息，后续两个节点独立维护。日期按浏览器本地日历日显示到期提示，不改变实验资格、参数范围、桶位或容量。
- 新增 18 项元数据与保存保护测试，全部通过，覆盖有效日期及闰年、过去日期、旧存储加载、新层／子域创建、初始层独立副本、单节点更新、持久化再加载、模拟分流不变，以及外部存储变化／删除、迁移和读写失败。该阶段根代理运行全量测试 292/292 通过，0 失败、0 跳过，日志 `/tmp/abtest-v22-tests-final.log`。
- `saveTopology` 保存前比对内存配置对应的原始存储与最新存储，已检测到的旧页面写入被拒绝；表单保留输入。此机制是浏览器写前检查，不构成原子多标签事务。
- 最终集成复验时，共享目录中的并行 JSON 编辑工作已增加测试。修正 `jsonc-parser` 的运行时 `SyntaxKind` 与 ambient const enum 声明在现有编译选项下的导入兼容问题，未改解析行为或编译选项。当前全量 `npm test` 为 312/312 通过，0 失败、0 跳过，日志 `/tmp/abtest-v22-integration-tests.log`；本轮层域专项仍为上述 18 项，不将并行 JSON 功能计入本次交付。
- 最终 `npm run build` 通过，JS 1,022.53 kB（gzip 299.74 kB）、CSS 240.84 kB（gzip 46.66 kB），日志 `/tmp/abtest-v22-build-final.log`；保留既有单包超过 500 kB 提示。构建包含新建表单重复错误提示去重及字段错误定位修正。

### 浏览器验收

所有新增或修改测试配置均位于独立来源 `127.0.0.1:5173`，未向用户 `localhost` 写入层域测试数据。

1. 旧展示参数层初始显示负责人、期限“未设置”。补充信息默认陈思远／长期；清空负责人或切临时后不填日期时拒绝保存并显示字段错误。
2. 保存林青／临时／2026-09-01 后出现“已超期”，刷新仍保留；展示参数层继续为 9,500 桶联合占用、500 桶未占用。修改姓名和期限后取消，原林青与临时日期不变；再保存长期并刷新，日期显示“—”，超期提示消失。
3. 创建 `layer-77db01df`“资源信息验收层”。临时缺日期时创建按钮禁用；填写 2026-10-01 后创建成功，负责人陈思远，仍按既有规则显示空层“待配置”。复验字段错误仅显示一次，859px 表单的字段与固定底部按钮可读。
4. 在离散桶子域验收的初始层创建 `domain-501890c6`“临时资源信息验收域”，负责人周宁，临时至 2026-10-31，占父层 10%／1,000 桶／全局名义 0.56%。自动生成 `domain-501890c6-initial`；刷新后初始层同样为周宁／临时／2026-10-31，仍提供创建实验入口。
5. 第一页编辑初始层负责人为“保留输入验收”；第二页将其域改为赵铭／2026-11-30 并保存。第一页保存被明确拒绝，仍保留输入与旧日期。主动刷新后初始层仍为周宁／2026-10-31，证明域编辑未联动初始层；第二页显示赵铭／2026-11-30。1280px 详情卡片、编辑入口和日期说明可读。

### 范围

本轮验收新增一个空参数层、一个子域及其初始层，并修改测试展示参数层的管理信息；未新增或调整实验，测试来源正式实验仍为 36 项。没有增加退役、冻结、自动停用、自动回收或审批流程。负责人是原型内的姓名字段，未接入真实成员目录；到期提醒仅在页面显示，未发送外部通知。

## 2026-09-16：v23 大 JSON 参数编辑工作台

### 实现与自动验证

- 底层采用 CodeMirror 6 和 jsonc-parser。专业源码编辑器按需加载；参数目录、应用私有标签筛选、分组结构差异和草稿集成为平台业务代码。
- 新增 14 项 JSON 分析测试、6 项草稿校验／批量添加集成测试。覆盖数百行嵌套配置、JSON Pointer 转义、重复及转义同名字段、非有限数、过深嵌套、对象字段重排、数组整体变化、缺失与 null、原文保留及原型属性安全。
- 全量 `npm test`：312/312 通过，0 失败、0 跳过。日志 `/tmp/abtest-json-workbench-tests.log`。
- 最终 `npm run build` 通过，日志 `/tmp/abtest-json-workbench-build.log`。CodeMirror 动态模块 384.68 kB（gzip 124.63 kB），主 JS 1,040.38 kB（gzip 305.49 kB）；保留既有主包超过 500 kB 的构建提示。
- 格式化、结构比较、草稿提交检查和批量选参使用相同严格 JSON 规则；错误不通过标准解析的后值覆盖方式被静默消除。已有应用归属、参数层范围、类型和所有组 Key 一致性检查继续执行。

### 浏览器验收

使用独立来源 `127.0.0.1:5173`；未向用户 `localhost` 写入测试配置。新建草稿 `draft-d685392a-2dd2-4ec6-8ded-7cc35847fcc6`，名称“长 JSON 参数工作台验收”。用于编辑器验收，未选择完整实验范围或提交，正式实验数量仍为 36。

1. 输入 380 行配置，包含 12 个场景、每场景 4 条召回路，以及普通参数。参数目录识别顶层参数，搜索 `scene_12/timeout_ms` 得到唯一匹配；点击定位到第 349 行并滚入编辑视口。
2. A／B 两组配置只有一处 timeout 和一处召回数组内容变化。差异视图显示 2 处修改；数组完整并排展示，普通字段显示 80 → 120。点击“定位源码”回到第 349 行。比较只读，不应用合并或覆盖。
3. 输入嵌套重复 `timeout_ms` 后显示 1 个 JSON 问题。格式化被阻止，381 行原文保留；问题列表准确提示第 350 行第 9 列，并可定位。刷新后仍保留该非法草稿及重复错误。
4. 恢复有效文本后，查找 `rank-v3`、替换为 `rank-v4`，面板反馈替换 1 处。CodeMirror 的查找、替换及诊断提示采用中文。
5. 专注模式占满可用视口，提供分组切换、源码／差异切换、保存状态；背景不能操作。收起分组设置后不占源码编辑空间；展开后名称、流量与角色入口仍可用。859px 宽度下使用横向配置导航，参数工作台比旧侧栏布局获得更多宽度。

6. 回归时发现隐藏源码后恢复数值滚动位置会受虚拟行高测量影响。改用 CodeMirror 官方 `scrollSnapshot()` 后，A 组第 69 行、B 组第 349 行分别保留；在差异视图切换 A→B，再回源码，截图确认 B 视口恢复为第 336–362 行、光标仍在第 349 行。
7. 在 B 组把 120 改为 121，切换 A 组仍停留第 69 行，再返回 B 组并撤销，值恢复 120。两组光标和撤销记录没有串组。

### 边界

本次实现是源码编辑、只读目录导航和结构差异；没有引入固定基线／组覆盖、可编辑 JSON 树、字段 schema、CSV／文件导入或生产配置同步。数组按整体比较，不按业务 ID 推断合并。目录最多展示 5,000 个节点并明确提示，完整内容继续校验；未宣称已完成 1,000 个 Key × 20 组的容量或延迟压测。

光标、折叠、滚动和撤销记录在当前编辑会话中按分组保留；刷新恢复的是草稿原文、所在配置区及选中分组，不持久化编辑器历史。草稿仍受本机存储容量和已有版本冲突处理约束，生产多人协作能力未接入。
