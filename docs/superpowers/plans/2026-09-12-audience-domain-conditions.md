# 受众与域条件实施计划

v12 · 2026-09-12。用户已授权；实现及本轮原型验收完成。设计见[受众与域条件](../specs/2026-09-12-audience-domain-conditions.md)。

## 实施分工

1. 核心负责人：受众规则目录、三值资格判定、固定资格互斥证明、全区间冲突／并集、保存保护与测试。
2. 实验与层域界面负责人：域／实验受众选择、递归资格说明、固定画像模拟及冲突反馈。
3. 主执行者：受众管理、共享选择器、人工估算、App 状态校验及整体浏览器验收。
4. 独立审查与文档：检查动态属性、未知条件、版本漂移、非相邻区间及层保护边界，更新产品与架构说明。

## 已完成验收清单

以下清单结合核心自动化、独立代码审查与浏览器流程验收；具体浏览器证据见末尾。

- [x] 受众版本可创建与引用，域／实验展示继承 AND 后的资格；旧版本不可改写，既有引用不自动升级；不同系列同名拒绝，同系列新版本可沿用名称。
- [x] 缺属性为 unknown，未知旧受众不误执行或被当空集合；无效规则被阻断。
- [x] 相交的域—域、域—实验、实验—实验区间逐对判定；非相邻嵌套区间冲突不会漏检。
- [x] 只有同用户固定资格可证明互斥时复用桶，实时 platform 等可变属性不直接放行；占用并集不超过 100%。
- [x] 同 user_id 的资格快照固定并刷新恢复，缺属性不能再补填；坏快照／首次保存失败保留数据且停止模拟，天数拒绝小数；未匹配／未知不换桶补量，多候选同满足时拒绝执行。
- [x] 已运行／预留及祖先域资格变更受保护，域更换父层或层改挂域不能绕过保护；旧 hash、参数层范围、pending、空层及私有标签回归保持。
- [x] 人工估算区分输入假设与实测数据，完整交集的桶内合格率口径清楚；无真实接入不产生曝光数字。
- [x] 独立自动化 132/132 通过、0 失败，TypeScript／Vite 构建与浏览器核心流程通过。

## 已确认浏览器结果

- 创建“中国用户验收” v1；EXP1211 新用户注册天数 ≤ 7 与 EXP1212 老用户注册天数 ≥ 8，在同层各占 100% 相同 `[0,100)` 桶，完成创建、审核和运行。界面显示坐标联合覆盖 100%、名义配置 200%。
- `user_219` 的 CN／注册 3 天固定画像只命中新用户；`user_98` 的注册 30 天画像只命中老用户；缺属性的 `missing_audience_v12` 不执行两项实验，其他并行层继续。同桶候选展示各自匹配原因。
- 在召回参数层创建“中国人群子域验收”，非重叠、100% `[0,100)`，固定引用 v1 的 `country IN CN`。创建同系列 v2 追加 `is_member = true` 后，旧域仍只引用 v1，两个版本均持久化。刷新恢复 `user_219` 首次保存的只读画像，无会员属性仍能进入旧 v1 子域。
- 人工估算 100 万 × 80% × 20% = 16 万，真实曝光显示“未接入”。

主执行者已独立复跑 132/132 测试和构建；详细运行与浏览器证据见[验证记录](../../VERIFICATION.md)。当前交付为本地原型，生产 SDK、资格快照服务及曝光接入不在本轮验收范围。
