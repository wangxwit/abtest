# 应用内参数维护实施计划

v9 · 2026-09-12。用户已授权；设计见[应用内参数维护](../specs/2026-09-12-service-owned-parameters.md)。

## 实施分工

1. 主执行者：应用目录、ServiceCenter 参数页签整合、可刷新页签路由、旧入口兼容与目录认领区。
2. 参数组件负责人：ParameterCenter 收敛到服务范围，注册固定服务，搜索／筛选／批量维护只处理当前应用参数。
3. 文档与独立审查：更新 README、PRD、架构和联动说明，仅只读审查代码中的路由／作用域风险。
4. 主执行者：集成测试、构建、浏览器验收；在 VERIFICATION 记录真实结果，不修改历史证据。

## 验收清单

- [x] 不存在全局“全部参数”页面或入口；应用详情完整参数页签可操作。
- [x] 所有应用页签和参数详情刷新仍定位正确；旧列表回目录，旧 Key 定位所属服务。
- [x] 未归属／未知 Key 在目录内反馈；认领区只在历史未归属参数存在时显示。
- [x] 显式跨服务 Key、无效服务 ID、未知 Key 不会显示或修改其他参数。
- [x] 切换应用后参数选择与批量标签保持当前服务范围；无法沿用先前应用的勾选写入。
- [x] 注册服务固定当前应用，新增参数进入其参数列表，取消不生成参数。
- [x] 项目标签可跨服务复用；创建实验仍可跨服务按标签选参，且严格遵守层范围。
- [x] 标签变化、认领及导航不改变旧实验固定 Key、版本值和流量分配。
- [x] 全部自动化测试与构建通过；浏览器覆盖核心路径及实际验收视口，记录未覆盖项。

本轮集成验收完成，证据见 [VERIFICATION](../../VERIFICATION.md)。自动化 88/88、构建及核心浏览器路径通过；跨服务选参与取消参数注册由现有回归及代码审查确认，本轮未重跑完整实验创建流程。旧 v8 验证保持历史定位。
