# 交付校验记录

日期：2026-09-23 · 设计 v1.2 · [返回总览](./README.md)

本轮更新 Namespace、成员及资源权限，并整理仓库全部文档。原型代码未改动，设计中的权限能力尚未实现。

## 本轮更新

| 范围 | 结果 |
| --- | --- |
| 空间与身份 | 单企业部署；Namespace 承接旧 project；全局账号可加入多个 Namespace；成员不自动获得资源权限 |
| 细粒度授权 | 角色动作 + 资源归属范围 + 可选环境/到期时间；引用不继承权限，多资源操作逐项检查 |
| 服务端契约 | 同步表结构、管理 API、服务凭证、SDK、事件及指标查询；新增权限专章 |
| 分桶兼容 | 仅把第三字段名改为 namespace_id；保留 ID、盐、九字段编码和 v2 协议 |
| 文档表达 | 关系用图、归属用树、规则用表、操作用步骤；统一阅读入口和主文档引用 |
| 历史材料 | 精简早期设计与计划，标明替代关系；原始逐版验收证据归档保留 |

## 实际检查

| 检查 | 结果与边界 |
| --- | --- |
| Markdown | 36 份 Markdown 代码围栏闭合，221 个本地文件链接目标存在 |
| JSON | 28 个 JSON 代码块严格解析通过，拒绝重复键和非有限数；不等同于接口 Schema 验证 |
| Mermaid | 14 张图通过解析及浏览器 SVG 渲染；抽查总览与权限关系图，文字可读 |
| 分桶向量 | Node 编码器复核 10 组 payload、SHA-256、64 位无符号值和桶号；拒绝孤立 Unicode surrogate 和旧协议标识 |
| 向量兼容性 | 与上次提交逐组比较：输入值、编码、摘要和桶号均未变化 |
| 交叉校对 | 核对权限动作、归属继承、草稿保存/提交、发布复查、幂等作用域及撤权边界 |
| 变更范围 | 无原型源码、依赖或业务测试修改；git diff --check 通过 |

分桶样例复验命令：

```bash
node docs/服务端技术方案/verify-hash-vectors.mjs
```

脚本只验证字节编码和 hash 样例，不覆盖完整分流、资格服务、验签、跨服务上下文或埋点。Java／Go／移动端仍须实现并通过同一组向量。

## 尚未验证

- SQL 是设计片段：未执行建库、权限迁移或指标作业。
- API 和 SDK 是待实现协议：未做真实服务鉴权、并发、故障及性能测试。
- 本轮未重跑原型测试和构建；此前结果见[原型验证记录](../VERIFICATION.md)，不能当作本轮执行结果。
